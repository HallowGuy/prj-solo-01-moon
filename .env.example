# Identifiants PostgreSQL (à ajuster pour votre déploiement)
POSTGRES_USER=saltcorn
POSTGRES_PASSWORD=change-me
POSTGRES_DB=saltcorn

# URL de connexion pour Saltcorn (pas d'expansion automatique des variables ci-dessus)
# Format : postgres://USER:PASSWORD@HOST:PORT/DB
DATABASE_URL=postgres://saltcorn:change-me@postgres:5432/saltcorn
